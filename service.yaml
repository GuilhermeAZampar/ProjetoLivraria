apiVersion: v1
kind: Service

metadata:
  name: livros-api-service

spec:
  selector:
    app: livros-api

  type: LoadBalancer

  ports:
    - port: 80
      targetPort: 8000
      nodePort: 32489


